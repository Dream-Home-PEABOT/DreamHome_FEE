import React from 'react';
import { render, screen } from '@testing-library/react';
import ReactDOM from 'react-dom';
import userEvent from '@testing-library/user-event';
import { BrowserRouter } from 'react-router-dom';

import Report from './Report';

describe('Report', () => {
   it('should display a report of the user results', () => {

   });

   it('should have buttons for selecting a plan', () => {

   });

   it('a user should be able to click those buttons', () => {

   });

   it('should change the values in the report when a different button is selected', () => {

   });

})
